<!DOCTYPE html>

<html xmlns:th="http://www.thymeleaf.org">

<body>

		<form action="/thoughts/save-child" th:object="${thoughtParent}" method="POST">
		<div th:object=${thought}>
		
		<select th:field="${thoughtParent.parentId}">
			<option th:each = "p : parents"
					th:value = "${p.parent.parentId}"
					th:text = "${p.parent.parentId}">
			</option>
		</select>
		
		<textarea type="text" placeholder="Enter Child Description" th:field="${thought.description}"></textarea>
		</div>
		<button type="submit">Create Child</button>
		
		</form>
	
</body>

</html>