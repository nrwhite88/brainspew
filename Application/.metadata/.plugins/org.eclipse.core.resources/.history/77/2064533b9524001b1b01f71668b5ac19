<!DOCTYPE html>

<html xmlns:th="http://www.thymeleaf.org">

<head th:replace="layouts :: header"></head>

<body>

		<form action="/thoughts/save-child" th:object="${thought}" method="POST">
		
		<input type="text" placeholder="Thought Name" th:field=*{description}>
		
		<textarea type="text" placeholder="Enter Child Description" th:field="${thought.description}"></textarea>
		
		<select th:field="*{employees}" multiple="multiple">
			<option th:each = "employee : ${allEmployees}"
					th:value = "${employee.employeeId}"
					th:text = "${employee.firstName}">
			</option>
		</select>
		
		<button type="submit">Create Project</button>
		
		</form>
	
</body>

</html>